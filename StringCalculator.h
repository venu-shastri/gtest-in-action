#include<iostream>
#include<string>
#include <vector>
#include <numeric>
#include <regex>

using namespace std;

//int Add(const char* input);
//void Add(const string &input);
void  Add(const string &input,void(*displayPtr)(int));
